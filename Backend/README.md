# Backend

This folder is reserved for the Spring Boot backend application.

The Spring Boot project should use Maven, and its `pom.xml` must be committed to GitHub so all developers use the same dependency definitions.
