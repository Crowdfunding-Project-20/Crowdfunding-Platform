# Frontend

This folder is reserved for the Next.js frontend application.

The frontend dependency manifest and lockfile should be generated by the team using the selected Node.js/npm version and then committed to GitHub.
